# Seven Steps — app rebuild

TanStack Start rebuild of the GitHub Pages trainer. The live static site
is still `../index.html` + `../clips/today.json`.

Daily clips still come from `../scripts/gen-today.cjs` (08:00 HKT), now
scored against `../clips/persona.json` on 4chan boards:

- `/biz/` trading
- `/g/` AI / jobs
- `/pol/` news / HK
- `/v/` LoL
- `/fit/` gym / MMA
- `/a/` anime
